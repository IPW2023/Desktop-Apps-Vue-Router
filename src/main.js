import { createApp } from 'vue'
import App from './App.vue';

/**
 * TODO 1: Import router
 */

const app = createApp(App);

/**
 * TODO 2:
 * make sure the app uses the router
 */

app.mount('#app')

